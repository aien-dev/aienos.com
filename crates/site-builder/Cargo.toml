[package]
name = "site-builder"
version = "0.1.0"
edition = "2021"
description = "Pure native Rust static site generator for aienos.com"

[dependencies]
maud = "0.26"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
